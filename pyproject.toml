[project]
name = "agentic-trader"
version = "0.1.0"
description = "Autonomous RSI(2) SPY swing trader via Robinhood Agentic MCP"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "yfinance>=0.2",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false
